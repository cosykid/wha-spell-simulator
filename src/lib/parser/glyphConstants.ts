/** Dictionary id for the region sign, which has extra geometry safeguards. */
export const REGION_SIGN_ID = 'region';

/** Minimum line spread required before flat ink can be treated as the region sign. */
export const REGION_MIN_LINE_SPREAD = 0.14;
