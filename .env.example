DATABASE_URL="postgresql://user:password@host/db?sslmode=require"

# Bearer token for the recognition examples API (/api/recognition/examples).
# Required for all GET/POST/DELETE access; if unset, the whole API is disabled.
RECOGNITION_WRITE_TOKEN=""
