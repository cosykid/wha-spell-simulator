DATABASE_URL="postgresql://user:password@host/db?sslmode=require"

# Bearer token for the training data API (/api/recognition/examples).
# Required for all GET/POST/DELETE access; if unset, the whole API is disabled.
TRAINING_DATA_API_TOKEN=""
